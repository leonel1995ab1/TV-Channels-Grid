import { IImageUpload } from '../interfaces/image-upload.interface';

export const EN_IMAGE_UPLOAD: IImageUpload = {
    button: 'Add Logo',
    invalidFormatValidation: 'Only .PNG images are supported',
    multipleFilesValidation: 'Only 1 file is supported'
}